# Shell Environment

- `cd` is aliased to `zoxide` (smart directory navigation with frecency-based shortcuts)
- to interact with aws cli, use aws-vault exec sso-prod-engineering. If working with the litten.us1.staging.dog cluster that we are working on to deploy sagemaker and hyperpod cluster, use aws-vault exec sso-staging-us1-lofty-account-admin
